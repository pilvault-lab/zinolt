import { RankingStudio } from "./_components/RankingStudio";

export const metadata = {
  title: "Ranking — Zinolt",
  description:
    "Turn a curated Top-N ranking into a 9:16 ascending-podium reveal video.",
};

export default function RankingPage() {
  return <RankingStudio />;
}
