import { FrameGrab } from "./_components/FrameGrab";

export const metadata = {
  title: "Frame Grab — Zinolt",
  description: "Paste a YouTube URL, get 30 × 0.5s frames cropped 9:16 for montage.",
};

export default function FrameGrabPage() {
  return <FrameGrab />;
}
